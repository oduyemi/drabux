export const Header = () => {
  return (
    <header className="sticky top-0 z-50 h-0 transition-all duration-300">
      {/* Header completely removed - navigation moved to sidebar */}
    </header>
  );
};
